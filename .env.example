# Server port (default: 3100)
PORT=3100

# No API keys needed — clients pass their own Fireblocks credentials via Bearer token
# Bearer token format: base64-encoded JSON: {"apiKey":"...","apiSecret":"...","env":"sandbox|production"}
